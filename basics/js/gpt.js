const myName = 'Владимир';
let myAge = 24;
const isLearningJavaScript = true;
console.log(`Мое имя ${myName}. Мне ${myAge}. Я хочу выучить JavaScript и это ${isLearningJavaScript}`);

if (age < 18) {
    console.log('Ты еще пиздюк');
} else if (age === 18) {
    console.log('Тебе только исполнилось 18')
} else {
    console.log('Ты уже бугай!')
}